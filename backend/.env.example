# Application Settings
APP_NAME="SynqX ETL Agent"
ENVIRONMENT="development"
LOG_LEVEL="INFO"

# Database (PostgreSQL)
# Option 1: Full URL
# DATABASE_URL="postgresql://user:password@localhost:5432/synqx_db"
# Option 2: Components (Recommended for K8s/Docker)
DATABASE_HOST="localhost"
DATABASE_PORT=5432
DATABASE_USERNAME="postgres"
DATABASE_PASSWORD="password"
DATABASE_NAME="synqx_db"
DB_ECHO=False

# Redis
# Option 1: Full URL
# REDIS_URL="redis://localhost:6379/0"
# Option 2: Components
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=""

# Security
SECRET_KEY="changeme_generate_a_secure_random_string"
MASTER_PASSWORD="changeme"
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# AI Services
GOOGLE_API_KEY=""
GOOGLE_AI_MODEL="gemini-3-flash-preview"

# Email (SMTP)
SMTP_TLS=True
SMTP_PORT=587
SMTP_HOST="smtp.gmail.com"
SMTP_USER=""
SMTP_PASSWORD=""
EMAILS_FROM_EMAIL="info@synqx.dev"

# OIDC / SSO
OIDC_ENABLED=False
OIDC_CLIENT_ID=""
OIDC_CLIENT_SECRET=""
OIDC_DISCOVERY_URL=""
OIDC_REDIRECT_URI="http://localhost:3000/callback"

# Engine Tuning
ENGINE_MAX_WORKERS=0
CELERY_WORKER_CONCURRENCY=0